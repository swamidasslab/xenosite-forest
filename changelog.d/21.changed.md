Post-3.10 cleanup: pin pyright to Python 3.11; use ``match`` for ``Site`` shape checks in forest rules. Audit found no remaining ``typing_extensions`` / ``tomli`` / ``Self`` TypeVar shims in live forest.
