[build-system]
requires = ["hatchling", "hatch-vcs"]
build-backend = "hatchling.build"

[project]
name = "xenosite-forest"
dynamic = ["version"]
description = "Metabolic Forest: enumerate metabolite structures from reaction rules"
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.10"
authors = [
    { name = "S. Joshua Swamidass", email = "swamidass@gmail.com" },
]
classifiers = [
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Topic :: Scientific/Engineering :: Chemistry",
    "Intended Audience :: Science/Research",
]
dependencies = [
    "rdkit>=2022.3.3",
]
keywords = ["metabolite", "metabolism", "rdkit", "xenosite", "metabolic-forest"]

[project.optional-dependencies]
network = [
    "networkx>=3.2.1",
    "tqdm>=4.66.0",
]
# Downstream predictors; not installed on CI (workflows use --extra network only).
# Marker keeps forest's requires-python>=3.10 resolvable; predict itself needs >=3.11.
predict = [
    "xenosite-predict>=0.4.2; python_version >= '3.11'",
]
# Pair-orbit oracle. Tests skip when it is not installed. Not a default dependency.
pynauty = [
    "pynauty",
]

[project.urls]
Homepage = "https://xenosite.org"
Documentation = "https://github.com/swamidasslab/xenosite-forest/blob/main/docs/usage.md"
Repository = "https://github.com/swamidasslab/xenosite-forest"
Issues = "https://github.com/swamidasslab/xenosite-forest/issues"
Paper = "https://doi.org/10.1021/acs.jcim.0c00360"

[project.scripts]
xenosite-forest = "xenosite.forest.bfs:main"

[dependency-groups]
dev = [
    "pytest>=8.1.0",
    "pytest-xdist[psutil]>=3.5.0",
    "pytest-timeout>=2.3.0",
    "pytest-cov>=5.0.0",
    "networkx>=3.2.1",
    "tqdm>=4.66.0",
    "hypothesis>=6.168.0",
    "towncrier>=24.0",
    "pyright>=1.1.414",
    "ruff>=0.16.8",
]

[tool.towncrier]
directory = "changelog.d"
filename = "CHANGELOG.md"
start_string = "<!-- towncrier release notes start -->\n"
underlines = ["", "", ""]
title_format = "## [{version}](https://github.com/swamidasslab/xenosite-forest/releases/tag/v{version}) - {project_date}"
issue_format = "[#{issue}](https://github.com/swamidasslab/xenosite-forest/issues/{issue})"

[[tool.towncrier.type]]
directory = "security"
name = "Security"
showcontent = true

[[tool.towncrier.type]]
directory = "removed"
name = "Removed"
showcontent = true

[[tool.towncrier.type]]
directory = "deprecated"
name = "Deprecated"
showcontent = true

[[tool.towncrier.type]]
directory = "added"
name = "Added"
showcontent = true

[[tool.towncrier.type]]
directory = "changed"
name = "Changed"
showcontent = true

[[tool.towncrier.type]]
directory = "fixed"
name = "Fixed"
showcontent = true

[tool.hatch.version]
source = "vcs"
# Tag releases as vX.Y.Z. On later commits: next patch + .devN + short commit
# e.g. after v0.1.0 → 0.1.1.dev3+gabc1234
tag-pattern = 'v(?P<version>\d+\.\d+\.\d+(?:[.-].+)?)'
[tool.hatch.version.raw-options]
version_scheme = "guess-next-dev"
local_scheme = "node-and-date"
fallback_version = "0.0.0"

[tool.hatch.build.hooks.vcs]
version-file = "src/xenosite/forest/_version.py"

[tool.hatch.build.targets.wheel]
packages = ["src/xenosite"]

[tool.hatch.build.targets.sdist]
include = [
    "src/xenosite",
    "tests",
    "examples",
    "docs",
    "LICENSE",
    "README.md",
    "CITATION.cff",
]

[tool.pytest.ini_options]
addopts = "--doctest-modules -n auto --timeout=120 --import-mode=importlib"
# Collect from src (not src/xenosite/forest) so importlib resolves xenosite.forest.*
# and xenosite stays a PEP 420 namespace package (no xenosite/__init__.py).
testpaths = ["tests", "src"]
pythonpath = ["src", "tests"]
consider_namespace_packages = true
doctest_optionflags = ["NORMALIZE_WHITESPACE", "ELLIPSIS"]
# Avoid collecting optional network helpers unless explicitly requested
norecursedirs = [".git", ".venv", "build", "dist", ".hypothesis"]
filterwarnings = [
    "ignore::DeprecationWarning",
    "ignore::xenosite.forest.unstable.UnstableWarning",
]
markers = [
    "profile: opt-in AtomTracker / metabolize timing benches (not a CI gate)",
    "regression: deferred bug, not correct behavior or out of scope",
]

[tool.coverage.run]
source_pkgs = ["xenosite.forest"]
omit = ["*/_version.py"]

[tool.coverage.report]
skip_empty = true
show_missing = true

[tool.pyright]
include = [
    "src/xenosite/refactor_poc",
    "tests/refactor_poc",
]
exclude = [
    "src/xenosite/refactor_poc/test.ipynb",
]

# POC only. Forest and examples stay out. test.ipynb is not Python source.
[tool.ruff]
target-version = "py310"
line-length = 100
src = ["src", "tests"]
extend-exclude = [
    "src/xenosite/forest",
    "examples",
    "src/xenosite/refactor_poc/test.ipynb",
]

[tool.ruff.lint]
# E/W pycodestyle, F pyflakes, I isort. Bugbear (B) is off: B905 wants
# zip(..., strict=) and that is a behavior change, not a quiet rule.
select = ["E", "W", "F", "I"]
# PLC0414 flags `import name as name`. refactor_poc re-exports use that
# form on purpose. PL stays unselected; ignore so a wider editor select
# does not report it.
ignore = ["PLC0414"]
