Site shape checks name AtomSite, DirectedBondSite, the shared bond/atom-pair frozenset, and BondPairSite. Those names are aliases, so the checks are predicates rather than match classes. Unused internal stamp and site helpers were removed.
