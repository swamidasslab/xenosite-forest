Promote the rewritten Metabolic Forest as the live ``xenosite.forest`` package (pre-swap tree archived under ``xenosite._archive_forest``). Design and performance notes live under ``docs/forest/``.

Retire directed ``bond_atom`` unique-edit (never a Site pattern). Dehydrogenation emits unordered atom-pair Sites (``site_kind="atom_pair"``, both bond endpoints). Rules declare ``site_kind`` and short ``_example_substrates``; meta-test asserts emit shape. Do not merge/tag v0.7.0 until that gate is green (see TODO.md).
